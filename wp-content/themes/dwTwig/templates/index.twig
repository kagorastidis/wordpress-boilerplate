{% extends "base.twig" %}

{% block content %}
<div class="container">
	<div class="row">
		{% for post in posts %} 
			{% include 'category.twig' %}
		{% endfor %}
	</div>
	<div class="tool-pagination">
		{% include 'pagination.twig' %}
	</div>
</div>
{% endblock %}